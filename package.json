{
  "name": "chatgpt-todo-app",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "overrides": {
    "zod": "3.25.7"
  },
  "scripts": {
    "build": "npm run build -w @todo/core && npm run build -w api",
    "dev:api": "npm run dev -w api",
    "dev:web": "npm run dev -w web"
  }
}
